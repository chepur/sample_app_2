<h1>Simcard Types</h1>

<div class="row">
  <% if @sim_types.any? %>
      <div class="container-fluid">
        <p><%= link_to 'New Type', new_sim_type_path, class: "btn btn-primary", role: "button" %></p>
      </div>
      <div class="container-fluid">
        <table class="table table-striped table-bordered table-hover orders-table">
          <tr>
            <th>Type</th>
            <th>Active true/false</th>
          </tr>

          <% @sim_types.each do |st| %>
              <tr data-href=<%= sim_type_path(st) %> data-toggle="tooltip" title="Show">
                <td><%= st.kind %></td>
                <td><%= st.active? %></td>
                </td>
              </tr>
          <% end %>
        </table>
  <% else %>
      <div class="panel panel-warning" align="center">
        <div class="panel-heading">
          <h3 class="panel-title">No sim types yet</h3>
        </div>
        <div class="panel-body" align="center">
          <p><%= link_to 'New type', new_sim_type_path, class: "btn btn-danger", role: "button" %></p>
        </div>
      </div>
  <% end %>

  </div>
</div>
